services:
  - type: web
    name: kdrop-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: TMDB_API_KEY
        sync: false
      - key: CONSUMET_BASE_URL
        sync: false
